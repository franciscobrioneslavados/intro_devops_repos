name: Build and Deploy

on:
  push:
    branches:
      - actividad_2_4
  workflow_dispatch:

env:
  PROJECT_NAME: intro_devops
  ENVIRONMENT: dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Test
        run: echo "Hello World"

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Test Deploy
        run: echo "Deploying..."
